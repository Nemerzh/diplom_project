# OS / Editor
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
*.swp
*.swo
.idea/
.vscode/
.cursor/
.history/
*.code-workspace

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/
cover/
*.egg-info/
.eggs/
*.egg
pip-log.txt
pip-wheel-metadata/
.venv/
venv/
env/

# Node / Frontend
node_modules/
dist/
build/
.vite/
.eslintcache
.parcel-cache/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# Docker / compose runtime
*.pid
*.seed
docker-data/
docker-compose.override.yml

# Local DB / dumps
*.sqlite3
*.sql.gz
*.dump

# Logs / temp
*.log
logs/
tmp/
temp/

*.pptx
build_pptx.py

# Локальні override-конфіги (не комітимо)
*.local.yml
*.local.yaml
*.local.env

# Local infra (docker save tar, kind pg-backup, experiments csv тощо)
infra/local/out/

# Auto-rendered маніфести (наприклад deploy/swarm/stack.rendered.yml із content-hash іменами)
*.rendered.yml
*.rendered.yaml

# Experiment artifacts (старі імена + майбутні теки)
metrics_snapshot.json
load_test_results.csv
results/
experiments/out/
# Якщо OUT_DIR/experiments CSV спрямовані в каталог experiments/ (не лише infra/local/out)
experiments/experiment_runs/
experiments/experiments.csv

# Terraform / IaC (future-proof)
.terraform/
*.tfstate
*.tfstate.*

# --- Політика репозиторію: у Git лише код (джерела, deploy, скрипти, конфіги) + кореневий README.md ---
docs/
presentation/

# Усі Markdown окрім ./README.md (мануали, записка, презентація як текст — лише локально)
*.md
!/README.md
